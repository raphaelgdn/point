#!/usr/bin/env groovy
pipeline {
    agent none
    stages {
        stage('Start VM WORK') {
            agent{
                label'POWER'
            } 
            steps{
                sh 'echo "Start VM WORK"'
                sh '/usr/bin/virsh -c qemu:///system start WORK'
            }
        }        
        stage ('verify the VM is running') {
            agent{
                label 'POWER'
            }
            steps{
                sh '/usr/bin/virsh -c qemu:///system list'
            }
        }
        stage('Update time clock') {
            agent{
                label 'WORK'
            }
            steps{
                commmand 'W32tm /resync /force'
            }
        }
    }
}