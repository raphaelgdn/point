#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('Start VM WORK') {
            agent{
                label'POWER'
            } 
            steps{
                sh 'echo "Start VM WORK"'
                sh 'ls -l'
            }
        }        
        
    }
}