#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('TESTE') {
            agent{
                label'POWER'
            } 
            steps{
                sh 'rm -r *'
                sh 'echo "Start VM WORK"'
                sh 'ls -l'
            }
        }        
        
    }
}