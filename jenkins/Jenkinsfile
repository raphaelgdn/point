#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('Start VM WORK') {
            agent{
                label'POWER'
            } 
            steps{
                sh 'rm -r *'
                sh 'echo "Start VM WORK"'
                sh 'ls -l'
            }
        }        
        
    }
}